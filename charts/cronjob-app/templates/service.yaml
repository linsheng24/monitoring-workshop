apiVersion: v1
kind: Service
metadata:
  name: pushgateway
  namespace: web
  labels:
    app: pushgateway
spec:
  ports:
    - port: 9091
      targetPort: 9091
  selector:
    app: pushgateway
  type: ClusterIP